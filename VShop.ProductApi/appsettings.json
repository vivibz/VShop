{ // Depois de mapear o objeto relacional devemos definir a string de conexão no appsettings

  //Precisamos informar isso para que o entity framework saber onde criar o banco de dados
  //Depois ir no arquivo program.cs e obter a string de conexão mysql

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=VShopDB;Uid=root;pwd=94386771;"
  }
}
